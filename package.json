{
  "name": "pokequery",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "ingest": "pnpm --filter @pokequery/data ingest",
    "server": "pnpm --filter @pokequery/server dev",
    "web": "pnpm --filter @pokequery/web dev"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "@types/node": "^22.5.0"
  },
  "packageManager": "pnpm@9.9.0"
}
